#include <gtest/gtest.h>

#include "include/concurrency/synchronizer/rcu.h"

using namespace atom::concurrence::synchronizer;

TEST(TestRCU, Test) {
    // RCU<int> rcu;
    // auto guard = rcu.read();
    // EXPECT_EQ(*guard, 0);
}
